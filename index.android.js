/**
 * Image Grid Example using ScrollView and react-native-router-flux
 * @flow
 */

import {
  AppRegistry,
} from 'react-native'
import ImageGrid from './ImageGrid'

AppRegistry.registerComponent('ImageGrid', () => ImageGrid)
